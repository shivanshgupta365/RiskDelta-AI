export async function resolveApiHandler({ feature, method, url, context }) {
  void feature;
  void method;
  void url;
  void context;

  return null;
}

export async function resolveWebApiHandler({ feature, method, request, context }) {
  void feature;
  void method;
  void request;
  void context;

  return null;
}

export async function renderPremiumPage({ feature, context, params }) {
  void feature;
  void context;
  void params;

  return null;
}
